{
  "name": "express-parse",
  "version": "1.0.0",
  "description": "user express parse server",
  "scripts": {
    "server": "cross-env NODE_ENV=production node ./app",
    "server:dev": "cross-env NODE_ENV=development node ./app"
  },
  "dependencies": {
    "express": "^4.16.4",
    "dotenv": "^7.0.0",
    "parse": "^2.4.0",
    "parse-dashboard": "^1.3.3",
    "parse-server": "^4.1.0",
    "semver": "^5.6.0",
    "async": "^2.6.2"
  },
  "devDependencies": {
    "cross-env": "^5.2.0"
  }
}
